{/* <script>
const feedGrid = document.getElementById('feed-grid');
const feedSingle = document.getElementById('feed-single');
const cardView = document.getElementById('card-view');
const gridView = document.getElementById('grid-view');

// Cuando se seleccione la vista de cuadrícula
feedGrid.addEventListener('change', () => {
    cardView.classList.add('d-none');
    gridView.classList.remove('d-none');
});

// Cuando se seleccione la vista de tarjeta
feedSingle.addEventListener('change', () => {
    cardView.classList.remove('d-none');
    gridView.classList.add('d-none');
});
</script> */}